<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web Calculator</title>
    <link rel="stylesheet" href="style/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>
    <div id="page-content" class="container d-flex gap-5 flex-column flex-md-column py-md-5 align-items-top justify-content-top">
        <div class="navbar navbar-expand d-flex justify-content-between align-items-center p-3">
            <h1>Web Calculator</h1>
            <button id="theme-toggle" class="btn btn-primary btn-lg">
                <i id="sun-icon" class="fas fa-sun"></i>
                <i id="moon-icon" class="fas fa-moon d-none"></i>
            </button>
        </div>

        <div id="page-content" class="container d-flex gap-5 flex-row flex-md-row py-md-5 align-items-top justify-content-top">
            <div class="d-flex flex-column flex-md-row align-items-center justify-content-center">
                <ul id="dropdown-menu" class="dropdown-menu position-static d-grid gap-1 p-2 py-4 rounded-3 mx-0 shadow w-220px"></ul>
            </div>
            <div class="d-flex flex-column flex-md-column w-100">
                <div id="form-container"></div>
                <div class="mt-3">
                    <h6>Result:</h6>
                    <div id="${id}-result" class="alert alert-secondary result" role="alert">
                        <% if (locals.result) { %>
                            <%= result %>
                        <% } else if(locals.error) { %>
                            <%= error %>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="/js/operations.js"></script>
    <script src="/js/formManager.js"></script>
    <script src="/js/dropdownHandler.js"></script>
    <script src="/js/main.js"></script>
    <script src="/js/themeToggle.js"></script>
</body>
</html>



